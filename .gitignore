# Large / copyrighted content - never commit
third_party/
reference_mods/
upstream/
build/
dist/
backups/
local/
work/

# Accidental directories
--stats-only/

# Scratch / tool byproducts (Editor drops MapBuildStatistics.csv in cwd;
# test_*.tok are tok_parse experiment outputs; catalog_output.txt is a one-off dump)
/MapBuildStatistics.csv
/test_*.tok
/tools/catalog_output.txt
/scratchpad/

# build-speed: prefix snapshot cache (regenerable, per-machine; never commit)
.build_cache/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Python
__pycache__/
*.pyc
.venv/
