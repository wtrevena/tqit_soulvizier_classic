# whitelist_map.txt - KNOWN-INTENTIONAL map-contract deviations (suppressed).
# Format: "<CONTRACT-ID> <subject>" per line; "# ..." comments allowed.
# The subject must byte-match the violation's 'subject' field exactly.
# Every entry documents WHY the deviation is intentional / not our bug.

# --- MAP-GROUPS-1 -----------------------------------------------------------
# Two base-game / DLC shrine-binding groups inherited from the SVAERA base map,
# each with a single member that resolves to no placed instance in the merged
# world. Neither is our content and neither sits on the reachable campaign:
#  - Scandia01_02 is a Ragnarok (XPack2) teleport-shrine group.
#  - X4RespawnTestWorld is an Eternal Embers (XPack4) DEV test-world respawn
#    (never reachable in play). Both are base-inherited, not port regressions.
MAP-GROUPS-1 GROUPS/Scandia01_02 [TeleportShrine]
MAP-GROUPS-1 GROUPS/X4RespawnTestWorld [RespawnShrine]

# --- MAP-NAV-4 --------------------------------------------------------------
# NO ENTRIES (removed 2026-07-28, BL-b89-DEBT-4A). MAP-NAV-4 was demoted from a P0
# crash gate to a P2 SHAPE ADVISORY: it had been authored from the b87 reading that
# the Frida probe's `navOK=0` at ENTER meant ProcessRLTD had REJECTED the navmesh,
# and the 2026-07-27 runtime captures REFUTED that (navOK=0 at ENTER is the normal
# in-progress state; LEAVE flips it to 1). The real crash class was the malformed
# 148-byte REC02 body, now gated at P0 by MAP-NAV-5 / MAP-NAV-6.
# Its two former entries - `Levels/World/xBloodCave/drxBC3.lvl` and
# `XPack\Levels\Secret_Place\RogueEncampment.lvl` - were suppressed here as "latent P0
# crashes"; that claim rested on the dead premise, so the entries are removed rather
# than re-justified. Both chambers still SHOW UP in the battery, now as P2 advisories
# with honest wording, so nothing is hidden and nothing blocks a build (P2 never gates).
# Do NOT re-add MAP-NAV-4 suppressions: whitelisting an advisory only buries it.
