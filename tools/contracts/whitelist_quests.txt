# Whitelist for contracts_quests.py (DOMAIN E: QUEST CONTRACTS).
#
# Format: one "<CONTRACT-ID> <subject>" per line; '#' begins a comment line.
# A listed (contract-id, subject) pair is suppressed from the returned violations.
# `subject` must match the violation's `subject` string exactly (e.g.
#   "open_bloodcave_portal.qst :: Action_GiveItem.titleTag").
#
# KNOWN-INTENTIONAL deviations: NONE at build27.
#
# The only real finding on the build27 baseline is the two placeholder tags
# (QST-TAG-PLACEHOLDER) in open_bloodcave_portal.qst -> BACKLOG B-SUPRA-NOTIFY-1.
# Those are a REAL BUG queued for the bug wave to fix, NOT an intentional
# deviation, so they are deliberately NOT whitelisted (the gate should keep
# flagging them until the notification tags are replaced with dedicated ones).
#
# Inherited native-quest dead references (dominated by sv_commonmechanics.qst's
# vestigial item-upgrade crafting) are reported as INFORMATIONAL only and never
# enter the returned violation list or affect the exit code, so they need no
# whitelist entry.
#
# ── BUILD36a P0 HOTFIX (Will 2026-07-12): walk-through travel portals removed ──
# Will's TRAVEL LAW stripped every walk-through/proximity teleport we authored from
# the canonical map (fix commit 0f08297), portal_olympianarena1 among them -
# bossarena.qst opened it by record name. The portal RECORD still ships in the
# RE-EVALUATED under R-246 (2026-08-13, native-device travel): portal_olympianarena1
# is PLACED AGAIN (canonical maze03 + catacube door entrances; the TESTHUB court), so
# the old "names a door no longer placed" justification is DEAD. The line SURVIVES on
# a different, disasm-proven ground: the record's class is the BORN-OPEN static
# GridEntrance (apply_svc_patches _make_portals_born_open_gridentrance; verify hook
# green), and Action_OpenDynGridEntrance / the unlock only drives the
# GridEntranceDynamic state machine (SetPortalIsOpen call sites: Close/Open/activate,
# Game.dll - docs/DYNGRID_GATE_RCA.md sec 3-5). On a static GridEntrance the action
# finds no Dynamic state to flip and no-ops; the doors are open from raw data with or
# without it. FOLLOW-UP (unchanged, registered in BACKLOG): a future bossarena.qst
# rebuild should drop the dead unlock action entirely, then delete this line.
QST-DOOR-UNLOCK bossarena.qst :: records/quests/portal_olympianarena1.dbr
