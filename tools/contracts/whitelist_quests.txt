# Whitelist for contracts_quests.py (DOMAIN E: QUEST CONTRACTS).
#
# Format: one "<CONTRACT-ID> <subject>" per line; '#' begins a comment line.
# A listed (contract-id, subject) pair is suppressed from the returned violations.
# `subject` must match the violation's `subject` string exactly (e.g.
#   "open_bloodcave_portal.qst :: Action_GiveItem.titleTag").
#
# KNOWN-INTENTIONAL deviations: NONE at build27.
#
# The only real finding on the build27 baseline is the two placeholder tags
# (QST-TAG-PLACEHOLDER) in open_bloodcave_portal.qst -> BACKLOG B-SUPRA-NOTIFY-1.
# Those are a REAL BUG queued for the bug wave to fix, NOT an intentional
# deviation, so they are deliberately NOT whitelisted (the gate should keep
# flagging them until the notification tags are replaced with dedicated ones).
#
# Inherited native-quest dead references (dominated by sv_commonmechanics.qst's
# vestigial item-upgrade crafting) are reported as INFORMATIONAL only and never
# enter the returned violation list or affect the exit code, so they need no
# whitelist entry.
#
# ── QST-DOOR-UNLOCK: bossarena.qst names portal_olympianarena1 ──
# HISTORY: 07-12 P0 stripped the placed doors ("names a door no longer placed");
# R-246 placed them again (justification moved to the static-class no-op ground);
# R-248 (2026-08-14) REVERTED every placed door - the born-open GridEntrance travel
# device class is GRAVEYARDED (in-game refuted twice; MODDING_PLAYBOOK sec 10/10a).
# RE-EVALUATED under R-248: BOTH grounds now hold simultaneously -
#   (1) the record is placed NOWHERE on either map variant (gate_doors_hub, the
#       graveyard tombstone gate, asserts this stays true), so bossarena.qst's
#       unlock action targets no placed instance and no-ops; and
#   (2) even if one were placed, the record's class is the born-open STATIC
#       GridEntrance and Action_OpenDynGridEntrance only drives the
#       GridEntranceDynamic state machine (docs/DYNGRID_GATE_RCA.md sec 3-5) -
#       nothing to flip, no-op either way.
# The RECORD stays in the arz (shared-record law). FOLLOW-UP (unchanged, BACKLOG):
# a future bossarena.qst rebuild should drop the dead unlock action entirely, then
# delete this line.
QST-DOOR-UNLOCK bossarena.qst :: records/quests/portal_olympianarena1.dbr
