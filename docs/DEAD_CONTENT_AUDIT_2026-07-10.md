# DEAD CONTENT AUDIT (2026-07-10)

> 4-lane byte sweep of the shipped build30.3 artifacts for Typhon-portal-class dead content (placed but can never activate in Custom Quest). Lanes: locked FixedItems / GROUPS bindings / mandatory campaign path / quest-object activation. Fix routing notes appended by the coordinator.


## LANE B - GROUPS BINDING AUDIT (respawn/teleport-shrine "dangling UniqueId" class)

### [MAJOR] NEW dead respawn shrine. respawn_towerofjudgement01.dbr (Class StrategicMovementRespawnShrine), placed in xpack/levels/area05_judgment/undergrounds/judgment_towerug_floor04.lvl at world (-2432, 3, -13
- WHY DEAD: The map merge dropped this instance's 16-byte UniqueId from the world GROUPS(0x11) Shrine_Respawn_Hades binding record (base=53 members, shipped=51; this uid is one of the two lost respawn uids). The 0x05 instance still renders, but because its UniqueId matches no RespawnShrine group member the respawn system never registers it as a rebirth point. Identical failure mechanism to the Lower Olympus respawn trophy. Byte-verified two ways (44-byte member-record parse AND boundary-independent raw-GROUPS substring search agree).
- PLAYER IMPACT: On the MANDATORY Immortal-Throne Hades-act descent through the Tower of Judgement. It is 1 of 5 respawn shrines on the tower/underground chain; the siblings on floors 2, 3, 5 and the OTHER floor-4 placement all bind correctly, so a player who dies while on floor 4 respawns at the floor-3 shrine instead of floor 4 - a repeatable one-floor progress loss exactly where players fight down the tower. Not campaign-blocking (adjacent floors are covered), but a real lost-progress-on-death annoyance on the main path.
- FIX: GROUPS re-bind (preferred): add UniqueId 32703cacc540... back into the Shrine_Respawn_Hades member block so the vanilla checkpoint works again. Alternative: de-place the instance to match the Lower-Olympus 'remove dead trophy' policy, but re-bind is better for a mid-dungeon respawn on the mandatory path. Needs a new spec (REMOVE_DANGLING_SHRINE_SPECS in tools/build_section_surgery.py currently lists only olympusfinal02).

### [MAJOR] The Lower Olympus respawn trophy Will asked to REMOVE is STILL in the shipped map. respawn_olympus_new.dbr (Class StrategicMovementRespawnShrine), placed in levels/world/olympus/olympusfinal02.lvl at 
- WHY DEAD: Same mechanism: its UniqueId was dropped from Shrine_Respawn_Hades in the merge (the 2nd of the 2 lost respawn uids that account for 53->51). It renders but binds nothing. IMPORTANT: the de-placement fix IS coded (tools/build_section_surgery.py REMOVE_DANGLING_SHRINE_SPECS, wired in tools/svaera_plus_portals.py ~L1097-1129), but it postdates every built map. mtimes: shipped dist/.../Levels.arc = 18:51; svaera_plus_portals.py = 19:46; fresh local/Levels_merged.arc = 19:48; build_section_surgery.py (defines the M6 removal) = 20:19. I confirmed by bytes that BOTH the shipped map AND the newest local build still physically contain respawn_olympus_new. No built artifact yet includes the removal.
- PLAYER IMPACT: This is the item Will believes is already fixed (a 'DONE means DONE' gap). It is still live for every subscriber on md5 74240222: a dead respawn shrine a player clicks and nothing happens, and it is the ONLY respawn shrine in OlympusFinal02, so Lower Olympus has no working rebirth point.
- FIX: De-place (already coded per Will's instruction) - but it REQUIRES a map rebuild + redeploy; the current shipped Levels.arc and even the latest local build do not contain it. Action: rebuild the map (the build asserts M6 touches olympusfinal02, so a fresh build will drop it) and redeploy, then re-verify the shrine is gone from the shipped arc before calling it done.

### [MINOR] Missing (not visible-dead) respawn point. respawntemplegreeceug02.dbr (Class StrategicMovementRespawnShrine) is placed + bound in the BASE game at xpack2/levels/celticheartlands/underground/hercynianf
- WHY DEAD: Not a dangling render - the merge dropped both the instance and its binding, so nothing renders there at all. Included per the audit's inverse-direction ask (a base-bound device our map neither places nor binds). Lower confidence this is unintended vs a deliberate SVAERA cut of that cave's shrine.
- PLAYER IMPACT: Ragnarok (Northlands) act, Hercynian Forest underground cave. Dying in that cave respawns the player at the previous working shrine rather than in/near the cave. Modest lost progress; no visible broken prop. Off the strict campaign gate.
- FIX: If matching vanilla is intended, restore the dropped 0x05 placement + its X2_CelticHeartland_respawners binding (re-inject like the rebirth-fountain fix). Otherwise leave as an accepted cut. Secondary priority.

### CLEAN BILL (this lane)
Coverage is strong: 372 of 375 placed flagged binding-devices (318 StrategicMovementRespawnShrine + 57 StrategicMovementTeleportShrine; 0 device instances had an unresolved Class, so nothing was missed) are correctly bound to their GROUPS record. The ONLY dead visible devices are the 3 in the Hades act, fully accounted for by the merge's binding-member deltas (Shrine_Respawn_Hades 53->51, Shrine_Teleport_Hades 5->4); no other region lost a bound device. Specifically CLEAN: Greece, Egypt, Orient, Ragnarok (except the one Hercynian cave note), Atlantis, and Eternal Embers respawn/teleport shrines are all intact vs base. SV-added content is healthy: the build18 rebirth fountain (respawntempleorient01, uid feeb4bc6) IS bound to Shrine_Respawn_Orient (that fix is live in the shipped map); the Garden-of-Merchants teleport shrine (teleportshrine_gom, uid e08e87ff) is bound to DRXShrineTeleport_Duister; helios + sparta teleport shrines bound; the SV 'New Group' 20-member respawn network, Greece_Maze_Respawn01, zRespawnSanctuary and DRXShrineTeleport_RogueEncampment are all healthy. The Bandari nomad teleport network is 4/4 groups byte-identical to base. Inverse danglers (group members whose uid matches no placed instance) are NOT a crash risk and NOT a bug: the base game itself ships ~60 such leftover respawn members + ~13 teleport ones and runs fine - the engine simply has nothing to bind for them (ours' handful of extra leftovers, uids 000ce281/be95996c/dbbd704c, follow the same harmless base precedent). NOTE (per instruction, not re-reported as a finding): the known sibling teleportshrineolympus01 (uid 3c007d48, olympusfinal02, world 1304.17,21.7,-2743) is CONFIRMED still placed + dangling (dropped from Shrine_Teleport_Hades) - which is why Lower Olympus also lacks a working rift/teleport gate; and the known Duister teleportshrineorient01 is out of this scan's scope because it is flags=0 with no UniqueId (never a GROUPS member; a different inert-device class, already tracked as B-PORTAL-3). FixedItemTeleport/FixedItemTeleportWithHitBox portals (69 instances incl. the Typhon->Rhodes gate) are correctly excluded - they bind via 0x14/engine hooks, not GROUPS, and belong to the portal lane.

### Verification evidence
Audited the exact shipped artifacts: dist/workshop/content/SoulvizierClassic/resources/Levels.arc md5 74240222c8b8aed6a4a8032fbd9e9b52 (matches the canonical 74240222) and mod DB SoulvizierClassic.arz md5 3f60574155... (matches 3f605741), vs base C:/Program Files (x86)/Steam/.../Titan Quest Anniversary Edition Levels.arc + database.arz. Parser reproduced the documented facts exactly (shipped Shrine_Respawn_Hades=51 / Shrine_Teleport_Hades=4; base=53/5), using the repo's own proven helpers (svaera_plus_portals._parse_groups, build_section_surgery 0x05 walk, arc_patcher). Extraction validated against known anchors: helios+sparta teleport shrines resolve to the correct groups in BOTH maps, the fixed HV01 fountain binds only in ours, and both Olympus shrines show the exact bound-in-base / unbound-in-ours pattern. The 3 dead devices are confirmed by TWO independent membership methods that agree perfectly: (a) 44-byte member-record enumeration and (b) boundary-independent raw-GROUPS substring search over the 202,856-byte shipped GROUPS blob. Complete member-count accounting closes the loop: the 3 still-placed lost members are exactly the base->ours deficit; every other lost member either has no placed instance (harmless) or is fully de-placed (the Celtic cave). Device Classes resolved from mod+base arz (0 unresolved). The Olympus-fix ship gap was proven by decompressing the fresh local/Levels_merged.arc (respawn_olympus_new still present) and cross-referencing file mtimes (shipped map 18:51 and local build 19:48 both predate the 20:19 REMOVE_DANGLING_SHRINE_SPECS edit). Root-cause tooling note for the owner: the existing contract_groups (MAP-GROUPS-1, tools/contracts/contracts_map.py ~L851) only flags a group when it is WHOLLY dead (resolved==0); it cannot catch a single unbound device among many bound ones, which is precisely why this whole class shipped - a forward-direction per-instance check (every placed StrategicMovement*Shrine's UniqueId must be a member of a same-category group) would gate all three. Read-only throughout; scratch scripts under the session scratchpad; heavy 2GB map decompressions staggered one at a time given the owner is in-game.

## LANE C - MANDATORY CAMPAIGN PATH WALK (data-side). Walked Greece start -> Hades end against the ship

### [MINOR] The build30.3 Olympus->Rhodes unlock (tools/build_quest_files.py:1336 _add_typhon_rhodes_unlock, shipped in Quests.arc entry 'quest that controls bosses and their doors.qst', map QUESTS registration i
- WHY DEAD: NOT dead - functional and verified. But OnLevelLoad only re-evaluates at level-load time. If the player is already standing in the portal's level when Typhon dies (token granted mid-level), the OnLevelLoad trigger already fired tokenless and will not re-fire until that level next loads. canReFire=1 + isResettable=1 + a retroactive already-granted token make it idempotent, so it WILL fire on the next load (save reload, town-portal round trip, or walking into the portal's sub-level from an adjacent one). Vanilla opened it instantly via the base-game campaign-completion engine hook that does not fire in Custom Quest - the exact hook the fix replaces.
- PLAYER IMPACT: On the MANDATORY path (this is the only route from the base game into Immortal Throne). A player who kills Typhon and expects the Rhodes portal open immediately may briefly see it still sealed and conclude the fix failed (risk of a repeat 'STILL broken' report). It resolves on any subsequent load of the Olympus level. During the owner's 20-minute test loop: kill Typhon, then if the portal is not open, exit and re-enter the Olympus summit (or reload the save) and it will be unlocked - that is the intended, accepted mechanism, not a failure.
- FIX: None required if the in-game test shows the portal opens on natural re-entry (expected). If instant-open at the kill is wanted, add ONE redundant kill-gated trigger to the same step mirroring the IT boss doors' own pattern: Condition_KillAllCreaturesFromProxy(records\Proxies Boss\Boss\BossProxy_20_Typhon_Titan.dbr) -> Action_UnlockFixedItem(xq00_olympus_portaltorhodes, canReFire=1). That opens the portal the moment Typhon dies with no reload, keeping the OnLevelLoad trigger as the retroactive safety net for existing saves.

### [COSMETIC] records/quests/questobjects/fixeditemtyphonportal.dbr (Class FixedItemTyphonPortal, locked=1), referenced by the IT boss-door controller xquest_controlsbossdoors.qst in the post-Hades Persephone step 
- WHY DEAD: NOT a progression gate and likely NOT actually absent. This is the terminal post-Hades ending portal. The record exists in both mod and base arz; the Hades levels in the map are vanilla-byte-identical, so behavior equals vanilla. The 0-hit is most plausibly a stored-path-variant miss (I deliberately did not spend a third full 2GB map decompression chasing the basename while the owner is in-game); it is not evidence of a real de-placement.
- PLAYER IMPACT: Post-victory only, and the non-DLC target audience is unaffected. Campaign completion (and the difficulty unlock) is driven by the Hades kill wired through init - set up all acts.qst (Action_CompleteQuestNow), NOT by this portal. The placed+unlocked endportal_hades gives a non-DLC 'Classic' player a working end portal regardless. Zero mandatory-path impact.
- FIX: None (non-blocker). Optional only if the owner wants the Hades ending byte-perfect: a one-off basename scan of world01.map for 'fixeditemtyphonportal' to confirm the instance, then re-place it if genuinely missing. Out of scope for the non-DLC arc, which ends cleanly at Hades.

### CLEAN BILL (this lane)
Every MANDATORY transition Greece->Hades verified data-side against the shipped build30.3 artifacts. Each link has an explicit quest driver, registered inside the engine's 256-entry QUESTS load window (idx shown), with all target records confirmed placed in world01.map (case-insensitive scan) and satisfiable conditions:

ACT 1 GREECE (internal gates, all quest-driven UnlockFixedItem, all placed): Helos guard door (quest 1/1c, idx 47), Leonidas door (quest 2), Gorgon doors (quest 4), Athens main gate (quest 5), Athens catacombs (quest 6). Scripted-scene battles (Tegea/Sparta/Delphi/Megara/Athens) are walk-through.
GREECE->KNOSSOS boat: quest 7 - knossos (idx 63) enables Action_BoatDialog on Athens_BoatmanToKnossos + Knossos_BoatmanBackToAthens (both placed). Boat conversation-teleport is vanilla base-game machinery.
KNOSSOS->EGYPT boat: quest 8 part i - greece to egypt (idx 65) enables Action_BoatDialog on Knossos_BoatmanToEgypt + Rhakotis_BoatmanBackToKnossos (both placed).
ACT 2 EGYPT internal: Rhakotis library (quest 8, idx 64), Memphis gems + Memphis->Thebes gate (quest 9), Thebes->Hatshepsut gate (quest 10), all placed.
EGYPT->BABYLON portal: quest 11 - protect the sickle in babylon (idx 49) unlocks Q11_Hatshepsut_PortalGateToBabylon + Q11_Babylon_PortalGateToHatshepsut + activater (all placed).
ACT 3 ORIENT->WUSAO: quest 13 - yellow emperor (idx 52, Jade Palace shield), quest 14 - wusao (idx 53) unlocks Q14_Changan_GateFromJadePalace + Q14_Changan_GateToTomb (placed).
ORIENT->OLYMPUS: scripted scene - typhon escape and orient telkine boss fight (idx 93) unlocks Q14_TyphonTomb_RockBridgeToPortal (placed) -> Olympus summit.
OLYMPUS->RHODES (the new build30.3 unlock): FULLY VERIFIED - see finding 1; functional with a one-load latency caveat only.
RHODES->HADES (Immortal Throne spine): xquest_controlsbossdoors.qst (idx 118, registry-loaded) drives EVERY IT door purely on KILL conditions (Condition_KillAllCreaturesFromProxy / Condition_KillCreature - these fire in Custom Quest): Grey Sisters kill -> Medea grove door (xq02, placed); Charon kill -> Styx doors (xq03, placed); Kerberos kill -> Tower-of-Judgement doors (xq04, placed); Skeletal Typhon kill -> Hades palace doors (xq06_typhon*, placed); Hades kill -> final Hades palace door (xq06_hades*, placed) + Persephone -> endportal_hades (placed, unlocked).
ELYSIUM gates: Elysian-Fields levels present; the ToJ->Hades-palace stretch through Elysium is GridEntrance geometry (no quest gate between Kerberos and Skeletal Typhon in the controller), so no dead gate.
HADES PALACE progression doors: the xq06_* set above, all kill-gated + placed.
SIDE gate spot-checks: Medea's grove (on the IT path) = grey-sisters-kill gated, placed. No quest-gated 'Secret Passage' found (Greece Secret Passage is geometric). All major IT quest-locked doors are the kill-gated xquest_controlsbossdoors set.
DELIBERATE / NOT flagged (per task + IT_ENDPOINT_AUDIT): IT->EE portal (x4_other_001_control_expansionportals.qst, idx 232) and IT->Scandia/Ragnarok boss-door portal are intentionally neutralized (only the single Scandia UnlockFixedItem excised; typhon + endportal_hades kept). DLC-owner continue-past-Hades is an accepted vanilla-inherited caveat, not a mandatory-path defect.

TOKEN RECONCILIATION: 15 of 199 distinct checked tokens are never granted by Action_BestowTriggerToken, ALL benign - deliberate never-complete sentinels (TokenYouCanNeverGet / xTokenYouCanNeverGet / x4_TOKEN_YOU_CAN_NEVER_GET, which keep the controller quests re-firing), xpack4/DLC tokens, or engine-kill-granted IT side tokens (xSQ13 Stygian Hydra, xSQ27 generals). NONE gates a mandatory transition. The mandatory token 'Olympus - Typhon Defeated' is granted by quest 15 (idx 54) and is the exact token the boss-door controller already evaluated for Typhon repeat-loot (live-context proof).

### Verification evidence
ARTIFACTS (all reconcile with task): dist/workshop/content/SoulvizierClassic/resources/Levels.arc md5=74240222c8b8aed6a4a8032fbd9e9b52; resources/Quests.arc md5=631a2b4dbe1c7ddf002f54a4c58e7b2e; database/SoulvizierClassic.arz md5=3f60574155d18f24a28658725093d699.

MAP PARSE (one heavy decompression each): world/world01.map decompressed to 2,095,222,129 bytes; sections 0x01/0x02/0x10/0x11/0x18/0x19/0x1a/0x1b present. QUESTS(0x1b) = EXACTLY 256 registrations; boundary idx 254=quests/x4_other_002_hcdungeon_control.qst, idx 255=xpack2/Quests/x2_StartQuest.qst (vanilla byte-parity, matches IT_ENDPOINT_AUDIT). Registration indices (all < 256, in load window): quest that controls bosses and their doors=68 (Rhodes host), xquest_controlsbossdoors=118 (IT doors), quest 15=54, quest 7=63, quest 8 part i=65, quest 11=49, quest 13=52, quest 14=53, scripted typhon escape=93, init set up all acts=28, x4 expansion portals=232, SV quests urder/open_bloodcave/widowletter/bossarena=97-100.
PLACEMENT (case-insensitive scan of world01.map): xq00_olympus_portaltorhodes PLACED x1 (single OlympusFinal02 instance - unambiguous unlock target); IT doors xq02_medea/xq03_styx_charon/xq04_toj_kerberos/xq06_hadespalace_typhon/xq06_hadespalace_hades ALL placed; Q11_Hapshetsut_PortalGateToBabylon + Q11_Babylon_PortalGateToHatshepsut + Q11_Hatshepsut_PortalGate_Activater placed; Q14_TyphonTomb_RockBridgeToPortal placed; boat NPCs Athens_BoatmanToKnossos/Knossos_BoatmanBackToAthens/Knossos_BoatmanToEgypt/Rhakotis_BoatmanBackToKnossos placed; Q01_HelosGuardDoor/Q05_AthensMainGate/Q09_MemphisGateToThebes/Q10_ThebesGateToTempleOfHatshepsut/Q14_Changan_GateToTomb placed; persephone_hades + endportal_hades placed. Only fixeditemtyphonportal.dbr exact-path missed (finding 2). Level keywords present in map: rhodes, elysian, hadespalace, styx, tartarus.

QUEST PARSE (shipped Quests.arc, 106 .qst): Rhodes unlock trigger parses cleanly in 'quest that controls bosses and their doors.qst' step 'BOSS: Change the Loot Drops From Telkines & Typhon on Repeat Battles' = Condition_OnLevelLoad(isResettable=1) + Condition_OwnsTriggerToken(tokenName='Olympus - Typhon Defeated') -> Action_UnlockFixedItem(fixedItem='records\\xpack\\quests\\objects\\xq00_olympus_portaltorhodes.dbr', canReFire=1); exactly 1 reference to the Rhodes record across all shipped .qst. Token 'Olympus - Typhon Defeated' granted by quest 15 - save olympus from typhon.qst via Action_BestowTriggerToken. IT controller xquest_controlsbossdoors.

## LANE A - LOCKED FIXEDITEM SWEEP (the exact Typhon class). Enumerated every placed FixedItem* instanc

### [CAMPAIGN_BLOCKER] records\quests\questobjects\q15_typhontomb_portaltoolympus.dbr (Class FixedItemTeleport, locked=1, staticPortal=1, textTag tagTyphonUGPortalToOlympus). Placed ONCE, flags=1 (uid-tracked), in Levels/Wo
- WHY DEAD: Born locked with NO unlocker: verified across ALL 6 quest arcs (mod Quests.arc + base Resources/XPack/XPack2/XPack3/XPack4 Quests.arc, 367 qst parsed + raw-byte-grepped) there is ZERO Action_UnlockFixedItem for this record. Its ONLY quest reference is Condition_UseFixedItem in the loaded 'quest 15 - save olympus from typhon.qst' (registry idx 54), which merely DETECTS the player using the portal; it never opens it. The mod's version of that quest is byte-identical to base. In the base campaign the portal is opened by the same engine-internal completion hook that failed for xq00 (documented in tools/build_quest_files.py _add_typhon_rhodes_unlock: 'unlocked in the base CAMPAIGN by an engine-internal hook that does NOT fire in a Custom Quest'). staticPortal=1 with no data-encoded destination confirms engine-internal pairing.
- PLAYER IMPACT: On the MANDATORY main-story climax path. Quest 15 flow is: complete Quest 14 -> USE the q15 portal to travel to Olympus -> fight Typhon -> (xq00 unlocks) -> Rhodes/Immortal Throne. With q15 dead, the player reaches the Typhon tomb (PrisonFinal01) and the portal to Olympus never opens, so the final Typhon fight is unreachable and the Greek/base campaign cannot be completed. This ALSO renders the shipped build30.3 xq00 fix moot, because the player can never reach Olympus to use xq00. Same class as the confirmed-broken xq00, so high confidence.
- FIX: Quest trigger, mirroring the existing build31 precedent _add_typhon_rhodes_unlock in tools/build_quest_files.py: append an Action_UnlockFixedItem(records/quests/questobjects/q15_typhontomb_portaltoolympus.dbr, canReFire=1) trigger to the loaded 'quest that controls bosses and their doors.qst'. CRITICAL TIMING DIFFERENCE from xq00: q15 must be open BEFORE Typhon dies (you use it to reach the fight), so it CANNOT gate on the post-kill 'Olympus - Typhon Defeated' token; gate it on Condition_OnLevelLoad (unconditional, idempotent, retroactive) or Condition_OnQuestComplete('Quest 14'). Shares the same engine-internal-destination in-game confirmation risk as the pending xq00 fix.

### [MAJOR] records\xpack3\scenery\atlantis\08tartarus\structure\infrastructure\tartarus_entrance_gate01.dbr (Class FixedItemDoor, locked=1). Placed 16 times, once in every Tartarus arena: XPack3/Levels/Tartarus/
- WHY DEAD: Locked FixedItemDoor with solid collision (no noObstaclesWhenLocked field). NO quest in any of the 6 arcs references it (verified). In vanilla these open via the Tartarus arena wave-clear engine system, which is not driven by any loaded quest, so the gates stay shut in Custom Quest. Confidence MEDIUM: the exact runtime arena mechanism could not be proven dead from bytes, but no loaded quest opens them, matching the engine-hook failure class.
- PLAYER IMPACT: Tartarus (the Atlantis-DLC endgame arena survival mode) is REACHABLE (its entry portaltotartarus.dbr is unlock-loaded), but the arena-to-arena gates never open, so the whole Tartarus arena progression dead-ends at the first gate. Optional endgame content, not the main campaign, but an entire mode is blocked, and it is a 16-instance cluster.
- FIX: Confirm the arena wave->gate mechanism in-game; if engine-driven and dead in CQ, add a Tartarus controller quest (Condition_KillAllCreaturesFromProxy per arena -> Action_UnlockFixedItem + Action_OpenDoor on that arena's gate), mirroring the Greek/xpack2 boss-door controller pattern already used in the mod.

### [MAJOR] records\xpack2\underground\muspelheim\landsend\landsenddoor01_locked.dbr (Class FixedItemDoor, locked=1, description tagDoorEgy01). Placed 4 times: XPack2/Levels/Muspelheim/LandsEnd01.lvl world (4232,
- WHY DEAD: Locked door, solid collision, NO quest opener in any of the 6 arcs (verified). Unlock relies on the base engine campaign hook, which does not fire in Custom Quest. Confidence MEDIUM (path-criticality not confirmed in-game).
- PLAYER IMPACT: Muspelheim/Lands End is on the Ragnarok (xpack2) main path (the Surtr approach). If this door gates the mandatory route it blocks Ragnarok progression; if it fronts a side chamber the impact is minor. Needs an in-game path check to confirm which.
- FIX: Confirm whether the door is on the mandatory Muspelheim route; if so, add a controller-quest Action_UnlockFixedItem + Action_OpenDoor keyed on the relevant boss kill / lever, or (if it is a decorative/side door) de-place it.

### [MINOR] The engine-internal-destination flag on the whole locked-teleporter family. ALL 47 locked teleporters (19 FixedItemTeleport + 20 FixedItemTeleportWithHitBox + 8 FixedItemTyphonPortal) carry staticPort
- WHY DEAD: Not confirmed dead, but flagged per the sweep's explicit ask: an unlocked static FixedItemTeleport still resolves its destination through the engine's internal campaign portal-pairing, which is the exact unverified property that keeps the shipped xq00 fix 'awaiting in-game confirmation'. Unlocking is necessary but may not be sufficient in Custom Quest.
- PLAYER IMPACT: The 46 non-q15 teleporters are unlocked by normal loaded quests and the mod is live, so they most likely resolve; but destination resolution could not be proven from static bytes for any of them. If the engine does not pair static portals in CQ, a teleporter can render, unlock, animate, and still teleport nowhere (the xq00 pattern). q15's future fix inherits this same pending risk.
- FIX: In-game confirmation, not a byte fix. When the xq00 in-game test is run, capture whether a quest-unlocked static portal actually teleports in Custom Quest; that result generalizes to the whole family. If static pairing is dead in CQ, the durable path is the Model C portal-master NPC (Action_BoatDialog with explicit world coords) already scoped in build_section_surgery.py, which encodes the destination in data.

### [MINOR] The Primrose secret cluster in XPack2/Levels/DarkLands/FjordNE.lvl: records\xpack2\scenery\primrose\primrose_elysiumdoor01.dbr and primrose_templedoor01.dbr (FixedItemDoor, locked=1) at world ~(5022,3
- WHY DEAD: Their controller z_primrosecontroller.qst DOES exist in the base XPack2 Quests.arc and contains the Action_UnlockFixedItem + Action_OpenDoor for these records, but it is NOT among the map's 256 QUESTS registry entries (verified: all its references resolve loaded=False), so the controller never loads and the lever/doors never fire. This is the exact 'quest exists in an arc but is outside the 256-entry load window' case the sweep was asked to check, and it is the one instance the sweep found.
- PLAYER IMPACT: The Primrose hidden Elysium easter-egg area (an optional Ragnarok secret) is permanently inaccessible. Low impact (optional secret).
- FIX: Add z_primrosecontroller.qst to the map QUESTS registry inside the 256-entry window (same mechanism as the build22 widowletter/urder/bossarena insertion), or de-place the cluster if the secret is out of scope.

### [MINOR] Two Asgard/Ragnarok locked doors with no loaded opener: records\xpack2\sceneryasgard\structure\gladsheim\gate01.dbr (FixedItemDoor, locked=1) 1x in XPack2/Levels/Asgard/Gladsheim01.lvl world (2915,26,
- WHY DEAD: Locked, solid collision, no quest opener in any of the 6 arcs. Note: Ylva's OWN cell (x2q05_ylvacelldoor.dbr) in the same basement IS correctly wired via the loaded x2q05_therescue.qst (registry idx 156, Action_UnlockFixedItem + Action_OpenDoor) and is NOT dead; these 8 generic celldoor01 are the surrounding cells and the Gladsheim gate, which have no opener.
- PLAYER IMPACT: The 8 celldoor01 are most likely decorative background prison cells around Ylva's (non-blocking). The Gladsheim gate may front a side area in Asgard. Low impact; worth a quick in-game glance to confirm none of the 8 cells holds required content.
- FIX: De-place the decorative cell doors (they never open and only add locked collision), or quest-open any cell that holds content; confirm whether the Gladsheim gate is on a needed route and controller-open it if so.

### [MINOR] records\xpack4\quests\item\doors\x4_hcdung_e_chaoskeydoor_b.dbr (Class FixedItemDoor, locked=1, FileDescription 'Unlocks after using 3 keys'). Placed 1x in XPack4/Levels/Special/HCDun02_Room01.lvl at 
- WHY DEAD: The loaded HC-dungeon controller x4_other_002_hcdungeon_control.qst (registry idx 254) wires Action_UnlockFixedItem + Action_OpenDoor for the chaoskeydoor _a variants (x4_hcdung_n/e/l_chaoskeydoor_a) but NOT this placed _b variant, so this specific instance has no opener (verified by raw-byte scan of the controller).
- PLAYER IMPACT: Inside the xpack4 hardcore/bonus endgame dungeon (optional). The _a progression doors ARE wired, so _b is likely a secondary/redundant door; low impact but a real dead gate.
- FIX: Extend x4_other_002_hcdungeon_control.qst to also unlock the _b variant, or de-place it if it is redundant with the wired _a door.

### [COSMETIC] Seven key-insertion FixedItemQuestObject records (locked=1, quest=1, DisplayAsQuestItem=1) that are referenced by LOADED quests only via Condition_UseFixedItem, never Action_UnlockFixedItem: records\x
- WHY DEAD: Flagged only because they matched the locked-and-no-Action_UnlockFixedItem filter. In practice their locked=1 is the standard TQ 'requires the specific key item' gate that the quest engine manages while the referencing side-quest tracks (x4_sq_104_the_council_of_three.qst and x2q05_therescue.qst/x2quest_controlsdoors.qst are all loaded and use normal, CQ-reachable conditions, NOT engine campaign hooks). Low confidence these are actually broken.
- PLAYER IMPACT: Likely none (normal key-slot puzzle behavior). Listed for completeness so the sweep is exhaustive; if the council-of-three or Dvergr side quests are exercised in-game and a keyslot refuses interaction, revisit.
- FIX: No change expected. If in-game testing shows a keyslot will not accept its key, add an Action_UnlockFixedItem on the relevant quest step; otherwise leave as-is.

### [COSMETIC] records\xpack3\scenery\atlantis\underground\tombstone.dbr (Class FixedItemQuestObject, locked=1, description literally 'Hogge' which is a placeholder string, not a text tag). Placed 1x in Levels/World
- WHY DEAD: A stray XPack3 (Atlantis) tombstone quest object placed inside a Greek base-game minidungeon, with a dev placeholder description and no quest reference anywhere. A cross-expansion merge artifact.
- PLAYER IMPACT: A locked, non-interactive tombstone prop sitting in a Greek cave. Non-blocking, just cruft that a curious player might click and get nothing from.
- FIX: De-place it (remove_0x05_instances_by_dbr on MonsterCave01B), consistent with the M2 stray-placement de-placements already in build_section_surgery.py.

### CLEAN BILL (this lane)
Of 361 locked FixedItem records (1116 placed instances of 619 distinct FixedItem records total across the 2282-level shipped map), only 17 records / 42 instances are dead; the rest are cleared with reachable unlock conditions: (1) 336 records have a LOADED Action_UnlockFixedItem in a quest that is confirmed inside the map's 256-entry QUESTS load window. This includes the entire Greek boss-door/portal network driven by 'quest that controls bosses and their doors.qst' (Q04 Arachne alter all 4 element variants, Q06 Alastor catacomb doors, Q07 Minotaur secretos/one-way doors, Q08 Rhakotis scroll room + statue, Q09 Sphinx/Scorpos portcullises, Q10 VoK, Q11 Hatshepsut door-to-portal-room + Babylon Chimera portcullises + Waterfall door, Q13 Jade Palace shield, and the build31 xq00 Olympus->Rhodes fix which I verified IS present at registry idx 68 on the 'Olympus - Typhon Defeated' token), plus the xpack2/xpack3/xpack4 boss-door controllers (x2quest_controlsbossdoors, xquest_controlsbossdoors, x3quest_controlsbossdoors, xpack3tartarusportal, x4_other_001_control_expansionportals, x4_other_001_control_doors) that fire on boss-kill conditions reachable in Custom Quest. (2) 4 records auto-unlock via LockedClassification + LockedRadius (Boss/Hero/Quest chests that open when the named monster class dies nearby), which is valid regardless of quests. (3) 46 of the 47 locked teleporters (the exact Typhon class) are unlock-loaded; only q15 is orphaned. Correction applied during verification: x2q05_ylvacelldoor.dbr initially looked dead but is correctly unlocked by the loaded x2q05_therescue.qst (idx 156) - it was only mis-flagged because that quest failed structured parse; a raw-byte rescan reinstated it. IMPORTANT SYSTEMIC CAVEAT (finding #4): all 47 locked teleporters, including the 46 cleared ones, use engine-internal static-portal destinations, an unverified-in-CQ property.

### Verification evidence
Audited the exact shipped artifacts by MD5: Levels.arc 74240222c8b8aed6a4a8032fbd9e9b52, Quests.arc 631a2b4dbe1c7ddf002f54a4c58e7b2e, SoulvizierClassic.arz 3f60574155d18f24a28658725093d699 (all match the task hashes). METHOD: (a) Discovered that in the .arz the record 'type' field equals the Class string, so built the FixedItem universe (4135 records across 13 FixedItem* classes) from base database.arz (74013 recs) + mod arz (50446 recs) by record_type filter with zero record decode. (b) ONE big decompress of the shipped world01.map (2,095,222,129 bytes) and walked all 2282 level 0x05 sections version-aware (blob magic byte-3 = version; v0e=56-byte base record, v0f/v11=72-byte base, +16 bytes when flags!=0; stored position is level-local, world = corner ints_raw[6,7,8] + local, per the double-validated formula in build_section_surgery.py). Walked 491,836 placed instances with 0 record-size misalignments across all levels (full structural validation of the walker). Enumerated 619 distinct placed FixedItem records / 1116 instances; 361 locked=1. (c) Decoded every placed FixedItem record for locked/LockedClassification/LockedRadius/textTag/staticPortal/destination fields. (d) Indexed every FixedItem reference across ALL 6 quest arcs (mod Quests.arc + base Resources/XPack/XPack2/XPack3/XPack4 Quests.arc, 367 qst files) with action/condition class context, and resolved the map's 256-entry QUESTS registry to a loaded set (256/256 resolved; mod overrides base for the Quests/ namespace). Classified each locked item as auto-unlock / loaded-quest-unlock / dead. ROBUSTNESS: detected 2 qst that failed structured parse (x2q05_therescue.qst, x2_mainquestsanitycheck.qst), raw-byte-rescanned both, and corrected one false-positive (ylvacelldoor). Raw-byte-grepped all 6 arcs for the headline record q15_typhontomb_portaltoolympus and confirmed it appears ONLY under Condition_UseFixedItem, never any unlock action, in both mod and base. Cross-checked the RCA against the mod's own build31 precedent (tools/build_quest_files.py _add_typhon_rhodes_unlock at ~line 1305), which documents the identical engine-hook-not-firing-in-Custom-Quest mechanism for the sibling xq00 portal and whose fix (verified present in the shipped Quests.arc) does not cover q15. Scratch scripts and cached JSON (universe/placements/records/quest_refs/classified) are in the session scratchpad; no repo files were modified (read-only honored).

## LANE D - QUEST-OBJECT ACTIVATION AUDIT

### [MAJOR] Boss Arena boss-spawn trigger is name-mismatched. bossarena.qst (mod Quests.arc, load-window idx 100) STEP 1 waits on Condition_EnterVolume(volumeRecord = records\quests\portal_olympianarena.dbr) to f
- WHY DEAD: records\quests\portal_olympianarena.dbr is placed in ZERO of the 2282 shipped levels (confirmed by full 0x05 string-table enumeration). The volume that IS physically placed in boss_arena.lvl is records\quests\volume_startolympianarena.dbr (Class=BoundingVolume), and NO loaded quest watches it. So walking into the arena fires volume_startolympianarena (nothing is listening) while the quest blocks forever on portal_olympianarena (which exists in the arz but is never instantiated). boss_satyrshaman and location_bossarenacenter both exist and location_bossarenacenter is placed - only the trigger-volume record name is wrong. Root cause is INHERITED from SV 0.98i: SV's own boss_arena.lvl places volume_startolympianarena while SV's own bossarena.qst watches portal_olympianarena (byte-verified against upstream/soulvizier_098i). The mod faithfully back-ported both halves of a pre-existing SV authoring bug.
- PLAYER IMPACT: Reach the SV Boss Arena (an OPTIONAL side area, not the mandatory campaign path) and it is empty - the Satyr Shaman arena boss never spawns, so there is no fight and no reward. Latent-dependent on the arena entrance (portal_olympianarena1 GridEntrance in Maze03/CataCube02) actually streaming the player in, which is a separate map-lane concern; the moment the arena becomes reachable this dead trigger costs a full test loop.
- FIX: Quest trigger, one line: in bossarena.qst STEP 1 change the EnterVolume volumeRecord from records\quests\portal_olympianarena.dbr to records\quests\volume_startolympianarena.dbr (match the volume the level actually places), then rebuild Quests.arc via tools/build_quest_files.py. No map rebuild needed. (Alternative: place portal_olympianarena.dbr at the arena mouth - heavier, needs a Levels rebuild.)

### [MAJOR] open_bloodcave_portal.qst (mod Quests.arc, idx 97) STEP 3 'Duister' access is dead. Trigger[0] 'Give OCV2 Access' waits on Condition_EnterVolume(records\drxmap\zgardenofmerchants\portmebiznitch\seen_o
- WHY DEAD: seen_ocv2_trigger.dbr is placed in ZERO levels. It belonged to the shared Garden-of-Merchants portmebiznitch warp level (startingfarmland06d) that SVAERA replaced without the SV warp records - the documented lost-GoM-entrance class (docs/SV_AREAS_AUDIT.md section 5). The token is therefore never bestowed, so the Duister merchant-access branch never advances.
- PLAYER IMPACT: The 'Duister' secret merchant / Garden-of-Merchants access never unlocks for the player. Optional SV content, not main path. This is the same root as the live BACKLOG item B-PORTAL-3 (all Duister portals broken) and the lost GoM warp - it is already known/tracked, surfaced here from the quest-object side for completeness.
- FIX: GROUPS/blob re-inject (map lane): restore the portmebiznitch warp + seen_ocv2_trigger volume into the SVAERA shared surface level, the same Random09A-style blob patch already used elsewhere. The quest side is correct; only the object placement is missing. Tracked with the GoM-entrance restoration.

### [MINOR] A broad class of BASE-GAME / Immortal-Throne scripted-scene and side-quest triggers ship with their static anchors dropped: ~15 distinct absent volume/location/proxy targets across base scripted scene
- WHY DEAD: These base quests live in the mod's Quests.arc but are inherited byte-for-byte; the referenced trigger volumes / quest-location markers / spawn proxies are absent because SVAERA's editor re-bake of the base Greece/Egypt/Orient/IT levels dropped them. The mod ships SVAERA's base levels unchanged and does not touch these levels, so this is a SVAERA-baseline degradation, NOT an SVC-introduced regression (the D3 all_sv de-placement was a different, records\all_sv namespace and did not touch these).
- PLAYER IMPACT: On the MAIN PATH the player loses scripted ambush flavor (Athens satyr ambushes, Great Wall charge, Thebes sandwraith vista) - the scenes simply do not play; progression is unaffected and areas stay completable. Optional IT side quests (xsq*) can stall on a missing kill-proxy or reward. None is a campaign blocker. DLC (xpack2/3/4) accounts for 51 more absent targets but is out of scope (campaign ends at Hades).
- FIX: Out of Lane-D fix scope (base content). If desired: a vanilla-TQAE vs shipped 0x05 diff on the affected base levels to quantify exactly what SVAERA dropped, then re-inject the missing markers/proxies per level. Recommend documenting as a known SVAERA-baseline limitation rather than chasing each.

### [MINOR] testquesttoopendoors.qst is a stray developer test quest shipped INSIDE the 256-entry map QUESTS load window (idx 101). It fires Action_UnlockFixedItem on Q04_GorgonOneWayDoor and Q14_Changan_GateFrom
- WHY DEAD: It is a leftover dev test (its literal name), duplicating door unlocks that 'quest that controls bosses and their doors.qst' already owns, and referencing Ragnarok DLC Surtr monsters never reachable in the IT-bounded campaign. It consumes one of exactly 256 load-window slots - the same finite window whose overflow was the widow-letter root cause (docs/QUEST_STATE_INJECT.md).
- PLAYER IMPACT: No positive function; wastes a precious load-window slot and could in principle open the Gorgon (Greece Act 1) or Changan (Orient Act 3) doors on an unintended condition (unverified). Ships sloppy on a total-conversion whose quest budget is tight.
- FIX: De-register it from the map's QUESTS(0x1b) load list (map-tooling in svaera_plus_portals.py build_ordered_quest_list) and/or drop it from Quests.arc, freeing a slot. Verify nothing legitimately depends on it first (it should not).

### [MINOR] widowletter.qst (idx 99) STEP 1 chest honor/dishonor sub-branch is mis-linked. It spawns records\drxmap\quest\chest_goldenchest_normal_03.dbr at location_treasurechest, but the three 'Block Chest' tri
- WHY DEAD: The spawned chest record and the watched UseFixedItem records are different paths, so opening the spawned chest never satisfies the block-chest condition. All records exist in the shipped arz (this is a name-linkage bug, not a missing record). Almost certainly inherited from SV 0.98i.
- PLAYER IMPACT: The 'greedily loot the widow's treasure chest before returning her letter -> she turns dishonorable' moral-choice flavor never fires. The core widow-letter chain is intact and completes: finalletter, widow_ling, trg_foundzhidan trigger volume and location_treasurechest are all placed, the reward records exist, and STEP 2 'Talk to Widow HONORABLE' grants the reward + CompleteQuestNow. Optional SV questline; only the flavor branch is degraded.
- FIX: Quest trigger: align the block-chest Condition_UseFixedItem itemRecord(s) with the actually-spawned chest record (chest_goldenchest_normal_03.dbr), or spawn the goldenchest_0X variant the branch watches. Low priority - the questline still completes.

### [COSMETIC] 'quest that controls boss chest swap.qst' (idx 67) Condition_UseFixedItem references records\item\containers\boss\bosschest13_chimera_epic.dbr.dbr (double .dbr). The double-extension record exists in 
- WHY DEAD: A path typo baked identically into both the quest condition and the arz record name - self-consistent but wrong; every other boss chest uses single .dbr. It is a Condition (detect chest use), so the only failure mode is that the chimera epic-difficulty repeat-chest swap condition keys off the malformed name.
- PLAYER IMPACT: Negligible - at most the Chimera epic-difficulty repeat boss-soul chest swap misbehaves on one of ~24 bosses. Really a DB-lane naming-hygiene item, surfaced here as a quest-ref typo.
- FIX: Rename the arz record and the quest condition to a single .dbr (records\item\containers\boss\bosschest13_chimera_epic.dbr) in build_svc_database.py / build_quest_files.py.

### CLEAN BILL (this lane)
Verified WORKING from raw bytes (explicitly cleared, not just unchecked):

TYPHON -> RHODES PORTAL (the build30.3 headline fix) is FULLY WIRED and token-verified end-to-end: records\xpack\quests\objects\xq00_olympus_portaltorhodes.dbr is placed in OlympusFinal02.lvl at world (1155.8, 90.1, -3194.2) as a FixedItemTeleport, locked=1, with a UID + 0x14 binding; its destination xq00_rhodes_olympusportaltarget.dbr is placed in Rhodes_CityFinal_01.lvl; 'quest that controls bosses and their doors.qst' STEP 1 trigger[4] 'SVC: Unlock Olympus Portal To Rhodes' fires Action_UnlockFixedItem(xq00) gated on OwnsTriggerToken('Olympus - Typhon Defeated'); and 'quest 15 - save olympus from typhon.qst' bestows exactly that token on KillAllCreaturesFromProxy(BossProxy_20_Typhon_Titan). Tokens match - the fix actually fires.

DEFINITIVE TYPHON-CLASS SCAN IS CLEAN: every placed, locked (locked=1) FixedItemTeleport / FixedItemDoor / DynGridEntrance under non-DLC namespaces is unlocked by a loaded quest (xq00 by q68; q11 babylon/hatshepsut portals by q49; q15 tomb->olympus portal by q15; xq02 medea portal by IT xq02). ZERO orphaned locked teleporters or doors remain - no other Typhon-class dead object exists.

urder.qst (secret place): fully wired - all teleport NPCs (portal to act 1, portal to hallway, warriv) and all trigger creatures (gitar3, yerk, akara/charsi/gheed, murderbunny, zilla) are placed; the zilla01/02/03 spawn-then-kill chain is self-consistent and all three records exist in the arz. (KillCreature on spawned creatures is valid at runtime, not a broken step.)

open_bloodcave_portal temple/door/portal core: every drxmap blood-cave door, portal and trigger it drives is PLACED and referenced - babtpl_waterfallroom_secretdoor, hc_treasurydoor02_boss ('Open Temple Exit Door'), door_bossroom_trap, the xprtl_bc2et/et2fn DynGridEntrances, trg_open_waterdoor/sanctuaryportal, and the q_highpriest_lone / q_shaman_lone / q_leinth_lone kill-proxies. The redundant KillCreature 'Fallback' triggers reference proxy-spawned creatures (valid at runtime). Static object-activation is sound; if B-TEMPLE-DOOR-1 persists it is a runtime proxy-membership issue, not a wiring gap.

Esti / hidden blood-cave chest (supra reward): activation chain intact - proxy_hidden_bloodcave_chest is placed, hidden_bloodcave_chest_01/02/03 and the supra_special reward loot record all exist in the arz, and STEP 4 wires UseFixedItem -> GiveItem(supra_special) -> DisableProxy. If it is not dropping in-game the issue is loot-table content (DB lane), not quest activation.

widowletter core reward path: finalletter, widow_ling, trg_foundzhidan volume and location_treasurechest all placed; dialog/reward records present in arz; completes.

MAIN-PATH Typhon tomb -> Olympus portal (quest 15) placed + wired; ALL base boss-door unlocks in quest 68 (Gorgon, Alastor, Minotaur, Scarabaeus, Pharaoh Guard, Scorpos King, Aktaios, Sand Wraith Lord, etc, STEP 2-13+) have their doors/fixed-items placed and referenced. No campaign-path quest-object blocker exists.

### Verification evidence
Artifacts pinned to the task hashes before any analysis: Levels.arc md5 74240222, Quests.arc md5 631a2b4d, SoulvizierClassic.arz md5 3f605741 - all three confirmed on the dist/workshop copies.

Method independently validated (the critical guard against false findings): the placed-object set was built from 0x05 INSTANCE iteration (17,306 records) and then cross-checked against the 0x05 STRING-TABLE union (17,372 records). The instance set is a strict subset of the string-table set (0 records in instance-set but not string-table), proving the instance parse missed nothing; the 66 extra string-table entries are inert records\all_sv de-placement residue (string present, zero live instances). Positive/negative controls probed directly in the raw map: portal_olympianarena1 PLACED in 6 named levels, q05_athensmaingate PLACED in AthensCity02, xq00_olympus_portaltorhodes PLACED in OlympusFinal02, vs athensbattle_satyrs_goto01 genuinely ABSENT (while tegea_satyrs_goto01 present) - confirming selective SVAERA drops, not a parser gap.

Coverage: decompressed the shipped world/world01.map once (2,095,222,129 bytes) and enumerated all 2282 level blobs (0 bad blobs), the QUESTS(0x1b) load window (exactly 256 entries), 2561 quest-namespace placed instances, and 17,372 distinct placed record paths. Parsed all 256 loaded quests resolving each to its real source arc (mod Quests.arc for Quests/*, base XPack/XPack2/XPack3/XPack4/Quests.arc for the xpack tail); 254/256 fully parsed (the 2 unparseable are base Ragnarok DLC quests x2Q05_TheRescue + x2_MainQuestSanityCheck, out of scope). Extracted every record reference tagged by action/condition class + field role, then cross-referenced both directions.

Role correctness: separated MAP-placed roles (door/fixedItem/dynGrid/proxy/location/volume - reliable 'absent = broken') from RUNTIME roles (creature/npc/person spawned by proxies/generators - NOT reliable) from DB roles (item/formula/skill - checked against the arz, not the map). This is what distinguishes the real bossarena volume mismatch from ~1400 false 'absent' item refs in sv_commonmechanics.

Cause attribution reproduced from upstream bytes: extracted SV 0.98i's own boss_arena.lvl 0x05 (upstream/soulvizier_098i) and confirmed it places volume_startolympianarena.dbr (not portal_olympianarena.dbr) while SV's own bossarena.qst watches portal_olympianarena.dbr - the mismatch predates the mod. bossarena.qst is byte-consistent MOD == SV098i; SVAERA has no bossarena.qst.

arz e


# COORDINATOR ROUTING (2026-07-10)
- Lane A q15-portal CAMPAIGN_BLOCKER claim: RECONCILE vs play evidence - Will has reached Olympus repeatedly, so either the engine DOES open locked boss portals in CQ (which contradicts the xq00 no-show) or he used another route; the fix wave must test rather than assume. Boat-dialog herald ships regardless (guaranteed).
- Ragnarok-act findings (Muspelheim door, Primrose, Asgard doors): MOOT-BY-POLICY if unreachable behind the neutralized IT->Scandia cap - VERIFY reachability before spending effort. Tartarus: check whether the Atlantis act is reachable in-mod; if yes, the arena gates matter (build32).
- Tower of Judgement dead respawn shrine (mandatory Hades path): fixed by M13 SD/GROUPS restoration (corroborates it). Olympus trophy: removal is local-only (b31c) - Steam still ships it; rides next map push.
- Boss Arena volume mismatch + widowletter branch + chimera .dbr.dbr typo + testquesttoopendoors deregistration: routed to DB/map lanes for build31.
- Duister OCV2 dead step: superseded by the Q2 portal-master NPC (direct Garden access).
