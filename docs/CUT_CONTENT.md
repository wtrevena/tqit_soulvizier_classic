# CUT CONTENT - declared-unreachable-by-design (for the map contract suite)

> The map reachability contract (task #30) requires every SV cluster level to have an inbound
> entrance OR be listed here with justification. Anything here is intentionally not reachable and
> should NOT be flagged as a broken/unreachable-area defect.

- **Cold Tombs** (`coldtombs*` levels): SV shipped these with NO 0x0a pathfinding geometry, so no
  navmesh can be generated by the standard pipeline, and no inbound binder exists in either SV or
  our merged map. ON HOLD per Will (task #36) - investigate whether it's a real dressed level
  (missing only its pathing layer → terrain-derived navmesh, a new capability) or an empty shell
  (→ content authoring, needs Will's design approval). Until then: intentionally cut.
- **Ocean scenery levels** (`ocean_extension*`, `ocean_extensionx*`): backdrop/scenery only, no
  walkable floor, no 0x0a geometry - never intended to be entered. Permanently cut.
- **Sparta Crypt L2** - NOW HAS AN INVENTED ENTRANCE (build25, a portal in the deepest
  Athens-battlefields catacomb chamber). No longer cut. Listed here only to note SV itself never
  gave it an entrance (zero inbound binders in pristine SV) - ours is invented.

(Everything else - blood cave, Uber Dungeon, Boss Arena, Garden of Merchants, Secret Place -
has a real entrance as of build27 and must pass the reachability contract.)
